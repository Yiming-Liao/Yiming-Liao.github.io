
/*
window.addEventListener('scroll', () => {
    const scrolledHeight = window.scrollY;
    const windowHeight = window.innerHeight;
    // 當使用者下滑超過畫面一半時
    if (scrolledHeight > 100) {
      document.querySelector(".div-opening").style.opacity = "0.2"; // 隱藏歡迎畫面
      document.querySelector(".div-opening").style.transform = 'translateY(100px)';
    } else if (scrolledHeight <100) {
        document.querySelector(".div-opening").style.opacity = "1";
        document.querySelector(".div-opening").style.transform = 'translateY(0)';

    }
  });
   */